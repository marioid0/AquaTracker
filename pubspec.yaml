name: myapp
description: "Policy Manager - Comprehensive policy management application"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.0-278.1.beta

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.0.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  # Optimize app bundle size
  generate: true
  
  # Assets optimization
  assets:
    # Add optimized images here when needed
    # - assets/images/